<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>WhisperSpace - Create Post</title>
        <link rel="stylesheet" href="/output.css">
    </head>
    <body>
        <wrapper class="w-full flex flex-col items-center min-h-screen bg-gray-50">
            <%- include('partials/header') %>
            <main class="w-full max-w-7xl px-4 flex flex-col items-center">
                <div class="py-12 w-full max-w-3xl">
                    <div class="text-center mb-8">
                        <h1 class="text-3xl font-bold mb-2">Create a New Whisper</h1>
                        <p class="text-gray-600">Share your thoughts with the community</p>
                    </div>
                    <% if(locals.errors) { %>
                        <div class="mb-8 bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
                            <%- include('partials/authErrors') %>
                        </div>
                    <% } %>
                    <div class="bg-white p-8 rounded-lg shadow-sm border border-gray-200">
                        <form action="/create-post" method="post" class="space-y-6">
                            <div class="space-y-2">
                                <label for="title" class="block text-sm font-medium text-gray-700">Title</label>
                                <input 
                                    type="text" 
                                    name="title" 
                                    id="title"
                                    placeholder="Give your whisper a title..." 
                                    class="w-full p-3 border border-gray-300 rounded-md focus:outline-none focus:ring-1 focus:ring-black focus:border-black"
                                >
                            </div>
                            <div class="space-y-2">
                                <label for="content" class="block text-sm font-medium text-gray-700">Content</label>
                                <textarea 
                                    name="content" 
                                    id="content" 
                                    rows="8" 
                                    placeholder="Express yourself..." 
                                    class="w-full p-3 border border-gray-300 rounded-md focus:outline-none focus:ring-1 focus:ring-black focus:border-black"
                                ></textarea>
                            </div>
                            <div class="flex gap-4 pt-4">
                                <button 
                                    type="reset"
                                    class="px-6 py-2 border border-gray-300 rounded-md hover:bg-gray-50 flex-1"
                                >
                                    Cancel
                                </button>
                                <button 
                                    type="submit" 
                                    class="px-6 py-2 bg-black text-white rounded-md hover:bg-black/80 flex-1"
                                >
                                    Share Whisper
                                </button>
                            </div>
                        </form>
                    </div>
                    <div class="mt-4 text-sm text-gray-500 text-center">
                        <p>Tips: Be clear, be kind.</p>
                    </div>
                </div>
            </main>
        </wrapper>
    </body>
</html>