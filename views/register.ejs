<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>WhisperSpace - Register</title>
        <link rel="stylesheet" href="/output.css">
    </head>
    <body>
        <wrapper class="w-full flex flex-col items-center">
            <%- include('partials/header') %>
            <main class="px-4 py-20 w-full max-w-7xl flex flex-col items-center justify-center">
                <% if (locals.user && locals.isAuthenticated) { %>
                    <%- include('partials/alreadyRegistered') %>
                <% } else { %>
                    <div class="border border-black px-8 pt-12 pb-6 rounded-md flex flex-col w-full max-w-[400px]">
                        <form action="/register" method="post" class="w-full max-w-[400px] flex flex-col gap-6">
                            <input class="p-2 rounded-md border border-gray-300" type="text" id="first_name" name="first_name" placeholder="First Name">
                            <input class="p-2 rounded-md border border-gray-300" type="text" id="last_name" name="last_name" placeholder="Last Name">
                            <input class="p-2 rounded-md border border-gray-300" type="text" id="username" name="username" placeholder="Username">
                            <input class="p-2 rounded-md border border-gray-300" type="password" id="password" name="password" placeholder="Password">
                            <input class="p-2 rounded-md border border-gray-300" type="password" id="verify_password" name="verify_password" placeholder="Verify Password">
                            <input type="submit" class="mt-5 cursor-pointer p-2 rounded-md bg-black text-white hover:bg-black/80" value="Register">
                        </form>
                        <div class="errors pt-6">
                            <%- include('partials/authErrors') %>
                        </div>
                    </div>
                <% } %>
            </main>
        </wrapper>
    </body>
</html>