<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>WhisperSpace - Login</title>
        <link rel="stylesheet" href="/output.css">
    </head>
    <body>
        <wrapper class="w-full flex flex-col items-center">
            <%- include('partials/header') %>
            <% if(locals.user) { %>
                <%- include('partials/alreadyLoggedIn') %>
            <% } else { %>
                <main class="px-4 py-20 w-full max-w-7xl flex flex-col items-center justify-center">
                    <div class="text-center mb-12">
                        <h1 class="text-4xl font-bold mb-4">Welcome to WhisperSpace</h1>
                        <p class="text-gray-600 max-w-md mx-auto">
                            Log in to your WhisperSpace account to continue sharing and connecting with the community.
                        </p>
                    </div>
                    <div class="border border-black px-8 pt-12 pb-6 rounded-md flex flex-col w-full max-w-[400px]">
                        <form action="/login" method="post" class="w-full max-w-[400px] flex flex-col gap-6">
                            <input class="p-2 rounded-md border border-gray-300" type="text" id="username" name="username" placeholder="Username">
                            <input class="p-2 rounded-md border border-gray-300" type="password" id="password" name="password" placeholder="Password">
                            <input type="submit" class="mt-5 cursor-pointer p-2 rounded-md bg-black text-white hover:bg-black/80" value="Login">
                        </form>
                        <div class="errors pt-6">
                            <%- include('partials/authErrors') %>
                        </div>
                    </div>
                    <p class="mt-6 text-gray-600">
                        New to WhisperSpace? <a href="/register" class="text-black hover:underline">Create an account</a>
                    </p>
                </main>
            <% } %>
        </wrapper>
    </body>
</html>